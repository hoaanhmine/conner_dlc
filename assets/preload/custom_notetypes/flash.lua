function opponentNoteHit(id, dir, type, sustain) 
    if type == 'flash' and not sustain then
        cameraFlash('game','FFFFFF',1,true)
	end
end

function goodNoteHit(id, dir, type, sustain) 
    if type == 'flash' and not sustain then
        cameraFlash('game','FFFFFF',1,true)
	end
end